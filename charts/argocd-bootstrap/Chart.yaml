apiVersion: v2
name: argocd-bootstrap
description: One-resource chart that creates the root ArgoCD Application. Used to sidestep kubernetes_manifest's plan-time REST requirement.
type: application
version: 0.1.0
appVersion: "1.0.0"
